# Dependencies
node_modules/

# OS
.DS_Store

# Python caches
__pycache__/
*.pyc

# Temporary files
tmp_audio/

# MCP-only generated artifact (re-buildable via npm run build-restricted-index)
mcp-server/_restricted-index.json

# Restricted municipality raw data (license unclear — keep local only)
data/sapporo/
site/data/sapporo/

# Local RAG PoC extraction inputs/outputs (rebuildable, may be large)
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep
data/chunks/*
!data/chunks/.gitkeep

# Local extraction quality reports (rebuildable)
reports/*
!reports/.gitkeep
.vercel
.env*.local
