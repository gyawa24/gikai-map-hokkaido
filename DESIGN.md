# 北海道議会情報マップ — デザイン方針

## コンセプト

**公共性・信頼感・見やすさ**

行政情報サイトとして、どの年代の住民にも信頼されるデザインを目指す。
「AIが作ったっぽい」汎用デザインを避け、北海道の公共情報サイトとしての固有性を持たせる。

---

## カラーパレット

| 役割 | カラー | 備考 |
|------|--------|------|
| プライマリ（濃紺） | `#1B3A6B` | 北海道らしい深い紺。ヘッダー・アクセント |
| プライマリ（中） | `#2A5298` | ナビゲーション・リンク |
| プライマリ（薄） | `#E8EEF7` | カード背景・ハイライト |
| ページ背景 | `#F4F6F9` | 薄いグレー |
| カード背景 | `#FFFFFF` | 白 |
| テキスト（本文） | `#1A202C` | 濃いグレー（コントラスト比 ≥ 7:1） |
| テキスト（補助） | `#4A5568` | 中グレー |
| テキスト（注釈） | `#718096` | 薄グレー（16px以下では使わない） |
| ボーダー | `#CBD5E0` | カード・セクション区切り |
| アクセントライン | `#1B3A6B` | ヘッダー上部の帯 |

政党・会派バッジは各色を控えめに使用（`bg-xxx-50 text-xxx-800` 相当）。
ただし、バッジの文字は必ずコントラスト比 4.5:1 以上を確保。

---

## タイポグラフィ

- **フォント**: `"Hiragino Sans", "Hiragino Kaku Gothic ProN", "Noto Sans JP", "Yu Gothic UI", sans-serif`
- **本文最小サイズ**: 16px（`text-base`）
- **補助テキスト最小サイズ**: 13px（それ以下は使用不可）
- **行送り**: 本文 1.7、見出し 1.3
- **見出し階層**:
  - H1（サイト名）: 24–28px, font-bold
  - H2（セクション名・市議会名）: 20–22px, font-bold
  - H3（カード内見出し・議員名）: 17–18px, font-semibold

---

## レイアウト

- **最大幅**: `max-w-5xl`（1024px）
- **モバイルファースト**: 1カラム → 2カラム（sm）→ 3カラム（lg）
- **内側余白**: カード `p-5` 〜 `p-6`、セクション間 `gap-4` 〜 `gap-6`
- **ページ上下余白**: `py-8` 〜 `py-10`

### カード

- 白背景 + 薄ボーダー（`border-gray-200`）
- ホバーで濃紺ボーダーに変化（`hover:border-[#1B3A6B]`）
- `rounded-lg`（大げさに丸めない）
- `shadow-sm`（控えめな影）

---

## UIコンポーネント方針

### ヘッダー
- 背景: `#1B3A6B`
- 上部に細い帯（`h-1`）を `#F7C948`（北海道のゴールド）で入れてもよい
- サイト名は常に左上に表示（ホームへのリンク）
- 市議会ページではパンくずリストを表示
- ナビゲーションはアンダーライン型（下線でアクティブ状態を示す）

### トップページ
- ヒーローセクション: サイト名・説明・対象市区を明示
- 市選択カード: 色分けせず、統一感のある濃紺アクセント
- 「このサイトについて」の説明を1〜2文添える

### 議員一覧
- フィルター・ソートは目立つ位置に配置
- カード内の議員名は `text-lg font-bold`（17px以上）
- ふりがなは小さくてよいが最低 `text-xs`（12px）以上
- 「議席番号」バッジは `#1B3A6B` の薄い背景
- 委員会タグは背景なし or 薄グレー（色を多用しない）

### フッター
- 背景: `#1B3A6B`（ヘッダーと統一）または濃いグレー
- データ出典リンクを明示
- 「公式情報は各市議会サイトでご確認ください」の免責文

---

## アクセシビリティ

- コントラスト比: 本文 ≥ 7:1、UIテキスト ≥ 4.5:1
- フォーカスリング: `focus-visible:ring-2 focus-visible:ring-[#2A5298]`
- タッチターゲット: 最小 44×44px
- セレクトボックスのラベルは必ず隣接して表示

---

## 避けるべきこと

- 赤・緑・青・黄を並べた「カラフルすぎるバッジ」
- `text-xs`（12px）以下のテキストを本文や説明に使う
- 白背景に薄いグレー文字（コントラスト不足）
- 過度なアニメーション（`hover:-translate-y-1` 程度まで）
- グラデーション背景、派手なシャドウ
